// git add . & git commit -m "Initial commit" можно замінити на git commit -am "Initial commit"

import { add as addFunction, minus, division, multiplication } from './utils.js'
import divisionFunction from './utils.js';


addFunction(45, 5);
minus (45, 10);
divisionFunction (16, 2);
multiplication (5, 5);

import { mango } from "./utils.js";


console.log(mango);
